@startuml
title Activité — Boutique et panier

start
:Voir la liste des plantes;
if (Voir une plante en détail ?) then (oui)
  :Ouvrir la fiche plante;
endif
if (Ajouter au panier ?) then (oui)
  if (Compte connecté ?) then (oui)
    :Enregistrer dans le panier du compte;
  else (non)
    :Garder le panier sur cet appareil;
  endif
  :Passer la Commande;
   if (Compte connecté ?) then (oui)
    :Choisir comment payer;
    :Valider la commande;;
  else (non)
    :Se connecter;
  endif
  :Consulter mes commandes dans le profil;
endif
stop

@enduml